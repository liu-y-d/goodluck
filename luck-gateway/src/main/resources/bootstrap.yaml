server:
  port: 88

spring:
  application:
    name: luck-gateway
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
    gateway:
      discovery:
      locator:
        enabled: true